export default function EnglishAppLogo({ className }) {
  return (
    <h3 className={`${className} font-bold hover:cursor-pointer"`}>
      English<span className="text-green-400">App</span>
    </h3>
  );
}
